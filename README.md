# Samples for the most important C# 8.0 features:

* default interface members
* asynchronous streams
* nullable reference types
* switch expressions
* using declarations
* null-coalescing assignment
